import AppLayout from '@/components/layouts/AppLayout';
import Head from 'next/head';

const Dashboard = () => {
	return (
		<AppLayout>
			<Head>
				<title>Dashboard - Load Order Library</title>
			</Head>

			<p>Meow</p>
		</AppLayout>
	);
};

export default Dashboard;
